upstream application {
    server 0.0.0.0:8080;
}


server {
    listen 80;

    location /hello/ {
        proxy_pass http://application;
    }
    
    location ^~ /uploads/ {
        root /home/box/web/;
    }

    location ~ .+\..+$ {
        root /home/box/web/public/;
    }
}